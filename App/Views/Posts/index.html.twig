{% extends "base.html.twig" %} {% block title %}Posts Page{% endblock %} {%
block body %}
<div class="">
  <h1 class="my-4 text-secondary">Posts</h1>
  {% for post in posts %}
  <h3 class="text-primary">{{ post.title }}</h3>
  <p>{{ post.content }}</p>
  <hr />
  {% endfor %}
</div>
{% endblock %}
